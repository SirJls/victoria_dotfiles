# -- Meta ---------------------------------------------------------------------
# -- File:   ${DOTFILES}/mutt/colors/jls.acc
# -- Author: SirJls - http://sjorssparreboom.nl
# -----------------------------------------------------------------------------

# -- Display ------------------------------------------------------------------

source ~/.mutt/colors/jls_day

set from      = "mail@sirjls.nl"
set sendmail  = "/usr/bin/msmtp -a JLS"
set mbox      = "+JLS/Archive"
set record    = "+JLS/Sent"
set postponed = "+JLS/Draft"
set trash     = "+JLS/Trash"

macro index S "<save-message>+JLS/Spam<enter>"

# vim:set ft=conf et sw=2 ts=2 tw=79:
